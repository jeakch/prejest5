#include <stdio.h>
int main()
{
	int x, y, max;
	printf("请输入x的值：");
	scanf("%d", &x);
	printf("请输入y的值：");
	scanf("%d", &y);
	if (x > y)
		max = x;
	else
		max = y;
	printf("max=%d", max);
	return 0;
}
